import React from 'react';

// Pages
import Feed from '../Feed';

const Home = () => {
  return (
    <section className="container mainContainer">
      <Feed />
    </section>
  );
};

export default Home;
